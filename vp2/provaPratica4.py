def quantidadeDeConsoantes(palavra):
    total = 0
    cosoantes = ["b","c","d","f","g"]
    for l in cosoantes:
        if l == cosoantes:
            total = total +1
    print(total)

palavra = input()

quantidadeDeConsoantes(palavra)